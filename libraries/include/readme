include 

Dependencies required by other libraries in this repo.

Exposes the Arduino API, declares several POD types that are 
more type appropriate and const-correct than those defined in 
the Arduino API, and provides utilities and shortcuts useful 
in the Arduino environment.
