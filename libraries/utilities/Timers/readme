This library defines two generic/reusable "timer" types. The `Timer' type is 
a simple interval timer. The `CommandTimer' type extends `Timer' by adding the 
ability to execute "Command" objects at user-defined intervals.
