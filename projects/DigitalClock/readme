This project demonstrates the implementation of a digital clock on an Arduino 
Uno with an attached LCD/Keypad Shield. The clock operates like a typical 
digital clock which can be set using the keypad and includes an alarm function.
